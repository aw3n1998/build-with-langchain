# 直接依赖（最低版本约束，便于人读）。
# 复现实测环境请用锁定版本：pip install -r requirements.lock（精确 ==，由本文件生成）。
langchain>=1.2.0
langchain-openai>=1.2.0
langchain-core>=1.3.0
langgraph>=1.1.0
langgraph-checkpoint-sqlite>=3.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
aiosqlite>=0.20.0
httpx[http2]>=0.27.0
openai>=2.0.0
faiss-cpu>=1.8.0
fastembed>=0.3.0
pymilvus>=2.4.0,<3.0
langchain-community>=0.3.0
rank-bm25>=0.2.2
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
RestrictedPython>=7.0
paramiko>=3.4.0
imageio-ffmpeg>=0.5.0  # 成片合成：自带 ffmpeg 二进制
edge-tts>=6.1.0  # 成片旁白 TTS（免费，需联网）
